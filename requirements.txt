# EasyOPD pinned requirements — verified environment (as of 2026-06-28)
#
# OS: Ubuntu 22.04 | Python 3.11 | CUDA 12.4 | NVIDIA driver 535.161.08+
# GPU: 8x NVIDIA H20 (96GB)
#
# INSTALL ORDER MATTERS: torch, flash-attn, xformers, vllm, sglang, and
# flashinfer require dedicated index URLs and --no-build-isolation. Prefer the
# one-click installer (scripts/install_easyopd.sh) over `pip install -r` for a
# clean setup. This file pins the top-level, directly-installed packages; the
# complete frozen environment (including transitive and CUDA runtime wheels) is
# provided separately as easyopd_requirements.txt.

# ---- PyTorch stack (index-url: https://download.pytorch.org/whl/cu124) ----
torch==2.6.0
torchaudio==2.6.0
torchvision==0.21.0
torchdata==0.11.0
triton==3.2.0
torchao==0.11.0

# ---- Attention / kernels (--no-build-isolation) ----
flash-attn==2.7.4.post1
flash-attn-4==4.0.0b17
xformers==0.0.29.post2

# ---- Inference / rollout engines ----
vllm==0.8.5.post1
sglang[all]==0.4.6.post1
# flashinfer index-url: https://flashinfer.ai/whl/cu124/torch2.6/
flashinfer-python==0.2.5+cu124torch2.6

# ---- RL / distillation core ----
verl==0.5.0
transformers==4.51.1
tokenizers==0.21.4
accelerate==1.13.0
peft==0.18.1
datasets==4.8.4
safetensors==0.7.0
huggingface_hub==0.36.2
tensordict==0.9.1
ray[default]==2.47.1
liger-kernel

# ---- Config, logging, tracking ----
hydra-core==1.3.2
omegaconf==2.3.0
wandb==0.25.1
tensorboard==2.20.0

# ---- Serving / API utilities ----
openai==2.6.1
aiohttp==3.13.3
fastapi==0.135.2
uvicorn==0.42.0
python-dotenv==1.2.2

# ---- Numerics / data ----
numpy==1.26.4
scipy==1.17.1
pandas==3.0.1
pyarrow==23.0.1
einops==0.8.2
sentencepiece==0.2.1
protobuf==4.25.9
antlr4-python3-runtime==4.9.3
sympy==1.13.1
tqdm==4.67.3
dill==0.4.1
codetiming==1.4.0
pylatexenc==2.10
pybind11==3.0.2
ninja==1.13.0
psutil==7.2.2
packaging==25.0

# ---- Reward / evaluation ----
math-verify==0.9.0
latex2sympy2_extended==1.11.0

# ---- Dev / tests ----
pre-commit
