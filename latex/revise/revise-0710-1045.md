# EasyOPD EMNLP 2026 System Demonstration 修改执行文档

用途：本文件直接交给 CodeBuddy，作为 `EasyOPD-v0702` 论文、`framework_0526.pptx` 框架图以及配套 demo artifact 的后续修改规范。

## 0. 已确认且不得擅自变更的事实口径

1. Step-wise OPD 实验中的 teacher 统一写为：

   > a GRPO-optimized Qwen3-4B as the teacher

   不得再写 Qwen3-8B，也不要简化成未经说明的 “Qwen3-4B teacher”。

2. OPSD 与 SDPO 的层级关系：

   - **OPSD**：on-policy self-distillation 范式/setting；
   - **SDPO**：本文在 OPSD setting 中实现和评测的具体方法。

   因此可以写 “the OPSD setting” 或 “on-policy self-distillation”，方法行、方法标签和结果表中必须写 “SDPO”。不得把 OPSD 当成具体算法名与 SDPO 混用。

3. 所有数值以当前 PDF 中的实验结果为准，不回退到更早版本：

   - Table 4 中 SOD 的 LCB-v6 为 **37.88**；
   - 对应 Avg. 为 **39.95**；
   - SOD 相比 response-level OPD 的 LCB-v6 提升为 **7.61 points**。

4. 全文 claim 必须按“当前代码确实支持什么、当前实验实际验证什么”来写。不得把设计目标、继承自 verl 的能力、未评测能力写成 EasyOPD 已经实证证明的原创能力。

5. OPD 统一称为 **on-policy distillation**，不得写成 online distillation。

---

## 1. 论文核心定位

全文应围绕下面这一句组织：

> EasyOPD is a method-oriented integration layer built on verl that localizes heterogeneous OPD supervision logic while reusing verl for distributed rollout and optimization.

EasyOPD 的贡献不是提出新的 OPD 算法，也不是重新实现一个分布式训练后端。建议只保留以下三项贡献：

1. **Unified supervision abstraction**：将 loss、rollout metadata、reward、alignment 和 teacher-side computation 组织为明确的扩展边界；
2. **Method-local integration**：使已实现的代表性 OPD 方法把监督逻辑放在 method module 中，同时复用 verl 的执行后端；
3. **Reproducible case studies and system evidence**：用 cross-tokenizer、OPSD/SDPO、step-wise OPD case studies，以及真实的开销和扩展成本指标验证系统。

不要把“某个 OPD 方法比另一个方法更强”写成 EasyOPD 的主要贡献。算法结果用于证明框架可以承载异构 supervision interface，而不是证明 EasyOPD 提出了更强的算法。

---

## 2. 投稿前必须完成的 P0 修复

在提交 PDF 前做全文搜索，确保以下内容为 0：

- `[X]`、`[Y]`、`[Z]`、`[N]`、`[M]`、`[K]`；
- `Figure ??`、`Table ??`、`Appendix ??`；
- `[define Avg@16]`；
- 无效链接 `https:youtu.be`；
- Step-wise teacher 的 `Qwen3-8B`；
- 把具体方法错误写成 `OPSD` 的位置；
- 对当前实验没有支持的 “agentic environments/tasks” claim。

必须检查：

- GitHub 链接在无登录状态下可访问；
- demo package 可下载并有真实 quickstart；
- 视频链接有效，或将 MP4 正确作为 supplementary material 提交；
- 论文写出软件 license；
- Appendix 引用均有对应内容；
- 主体正文不超过 6 页，appendix 不超过 2 页。

---

## 3. `framework_0526.pptx` 修改方案

### 3.1 对现有 Slide 1 的判断

当前图的三层结构是合理的，应保留：

- User Layer；
- EasyOPD Layer；
- verl Execution Substrate。

当前问题不是结构错误，而是：

1. 右侧 1/2/3 说明栏重复了主图信息，占用较多横向空间；
2. “Minimal hooks”只有名称，没有表现数据从哪里来、传到哪里；
3. Methods 中混合了范式名和方法名，尤其把 OPSD 当成了方法；
4. “Switch methods by changing YAML only!” 是绝对化宣传语；
5. “Unify OPD methods”容易被理解为所有 OPD 方法已被统一支持；
6. “Scalable execution / Efficient scaling / asynchronous multi-node”实质上主要是继承自 verl，而不是 EasyOPD 新实现的能力；
7. 当前图没有表现一个真实 run 从 YAML 到 diagnostics 的用户路径。

### 3.2 Slide 1 必改文字

将标题：

> EasyOPD: A Three-Layer Framework for Unified OPD

改为更准确的：

> EasyOPD: Method-Local OPD Supervision over a Shared verl Backend

如果希望保留较短标题，可用：

> EasyOPD: A Three-Layer Framework for OPD Integration

将右侧：

> Unify OPD methods.

改为：

> Localize method-specific supervision.

将：

> Switch methods by changing YAML only!

改为：

> Select released methods through YAML configurations.

将：

> Preserve verl as the stable execution substrate

改为：

> Reuse verl for rollout, optimization, and distributed execution

底部总结句改为：

> EasyOPD separates user-side invocation and method-specific supervision from verl-based execution.

### 3.3 Methods 区域必须区分“范式”和“方法”

不要继续在同一行混排 `GKD / MiniLLM / OPSD / SRPO / SimCT / SOD`。

建议改成两层：

- 第一行，supervision regimes：
  - Logit-based OPD
  - Cross-tokenizer OPD
  - On-policy self-distillation
  - Step-wise OPD
- 第二行，released implementations：
  - GKD / MiniLLM（仅当代码确实发布）
  - SimCT
  - SDPO
  - SOD

其中：

- OPSD 只能作为 “On-policy self-distillation” 范式标题；
- SDPO 才能作为具体方法标签；
- SRPO 若没有在当前 release 中可运行，不应出现在图中；
- 任何未发布或未完成的方法不要用与已发布方法相同的视觉样式。最稳妥做法是直接删除。

### 3.4 Hooks 区域增加真实数据边界

现有 `loss / rollout / reward / alignment / teacher sidecar` 可以保留，但应在上下箭头旁增加小标签：

- 从 verl/teacher 到 method module：

  > rollouts, logits, rewards, alignment metadata

- 从 method module 到 verl：

  > losses, weights, and training statistics

不要把 hook 画成五个完整算法组件。Caption 或正文必须说明：

> A hook is a dispatch boundary that routes data or control to a selected method module; the method implementation remains inside the module.

### 3.5 右侧说明栏的处理

论文版 Figure 1 建议删除右侧 1/2/3 重复说明栏，把释放出的宽度用于：

- 扩大 hook 数据流；
- 避免 `teacher sidecar`、`Reward Manager` 等文字换行和挤压；
- 将 User → EasyOPD → verl 的箭头画得更明确。

如果保留右侧说明栏，至少删除橙色营销式口号，并把三条说明压缩为：

1. Configure a released method.
2. Construct method-specific supervision.
3. Execute with verl.

### 3.6 必须新增 Slide 2：Running Example

不要试图把所有 running-example 信息继续塞入 Slide 1。复制当前 PPT 风格，新建一页，用于导出论文 Figure 2。

推荐布局：

#### 左侧 30%：真实 YAML + CLI

展示一个真实 SimCT 或 SDPO 配置。所有字段名和命令必须从当前 repo 复制，不得编造 API。

示意结构：

```yaml
method: simct
student: ...
teacher: ...
alignment: ...
```

下方放真实启动命令，例如：

```bash
<the exact command released in the repository>
```

#### 中间 35%：配置解析与 hook composition

用短流程表达：

> Config → Registry → SimCT module → alignment + teacher-sidecar + loss hooks → verl

必须强调不同方法只激活所需 hooks，而不是所有方法都经过全部 hooks。

#### 右侧 35%：真实 diagnostics/output

从真实训练日志截图或重新排版以下指标中的已实现部分：

- distillation loss；
- alignment coverage；
- valid aligned positions；
- skipped samples；
- teacher-side latency；
- rollout throughput。

只展示代码真实输出的指标名。若某项当前没有记录，删除，不得为了画图而虚构。

推荐 Figure 2 caption：

> A running example of launching SimCT with EasyOPD. The YAML configuration selects the method and resolves its method-specific options; the registry activates the required alignment, teacher-side, and loss hooks; verl executes rollout generation and optimization, while EasyOPD reports supervision-specific diagnostics.

---

## 4. Abstract：建议整体替换

下面版本已经落实 teacher/OPSD/claim 口径；方括号内容必须用真实 release 信息替换或删除。

> Conventional language-model distillation often relies on fixed teacher-generated data, which may not cover the states encountered by an evolving student policy. On-policy distillation (OPD) instead collects teacher or evaluator supervision on student-generated rollouts. However, existing OPD methods differ substantially in supervision form, tokenizer compatibility, teacher access, and supervision granularity, leading to fragmented implementations that are difficult to reproduce and extend. We present EasyOPD, an on-policy distillation framework built on verl, a distributed reinforcement-learning framework for large language models. EasyOPD separates user-side configuration, method-specific supervision logic, and verl-based execution. Its method modules connect to the shared backend through extension boundaries for loss construction, rollout metadata, reward processing, tokenizer alignment, and teacher-side computation. We implement representative methods covering logit-based OPD, cross-tokenizer OPD, on-policy self-distillation, and step-wise OPD. Experiments on reasoning, code-generation, scientific-knowledge, and tool-use benchmarks show that these implementations can be executed through the same verl-based backend while retaining their method-specific objectives and task-dependent performance profiles. We release EasyOPD with runnable YAML configurations, documentation, an installable demonstration package, and a demonstration video.

注意：

- 删除 “agentic tasks”；当前证据更适合写 “tool-use benchmarks”；
- 不写 “reproduce reported performance” 除非增加 original implementation 与 EasyOPD 的逐项对照；
- 不写 “standardized comparison under a unified evaluation protocol”，因为三类 setting 的模型、数据和指标不同；
- 如果没有测量 raw verl vs EasyOPD overhead，不在 Abstract 中写低开销；
- 如果某类方法没有随 repo 发布，则从 “We implement ...” 中删除。

---

## 5. Introduction 修改规范

### 5.1 缺口表述

当前：

> current frameworks do not jointly provide first-class interfaces for diverse OPD supervision, method-local extensibility, reproducible invocation, and asynchronous multi-node execution.

建议改为：

> Existing frameworks address complementary parts of the OPD workflow, including trainer-centric distillation APIs, scalable rollout and optimization, and teacher–student execution. However, we find no single reviewed framework that documents method-local extension boundaries across the heterogeneous supervision interfaces considered in this work while reusing a common distributed backend.

原因：避免把“异步多节点”包装为 EasyOPD 独有能力，也避免对其他框架作绝对否定。

### 5.2 框架修改范围

当前：

> without per-method changes to the core training stack

如果代码 diff 能证明 evaluated methods 均未修改 core trainer/worker，可写：

> For the evaluated implementations, method-specific logic is placed in registered modules after the shared hook layer is introduced, without additional method-specific branches in the core trainer or workers.

如果任何方法仍修改 core trainer/worker，则必须按实际情况写，不能保留该 claim。

### 5.3 支持范围

当前对 black-box、rubric-based、agentic 的表述需要分级：

- 已发布且实验验证：使用 `we implement and evaluate`；
- 已发布但未在本文实验验证：使用 `the interface includes support for`；
- 只是设计上可能容纳：使用 `the extension boundaries are designed to accommodate`；
- 未发布：从贡献和架构标签中删除。

不要用一个 `supports` 同时覆盖这四种状态。

### 5.4 Introduction 末段的实验总结

删除所有 `[X/Y/Z/N/M/K]`。如果还没有系统测量结果，使用：

> We evaluate EasyOPD through three case studies covering cross-tokenizer OPD, on-policy self-distillation, and step-wise OPD. Within each setting, methods share the model initialization, training data, execution backend, and evaluation procedure where applicable, while retaining their original supervision designs. We further report framework overhead and integration cost relative to matched verl implementations [only if these measurements are actually included].

最后一句：

> We release the code under the [actual license] with runnable configurations, documentation, an installable demonstration package, and a demonstration video.

---

## 6. Section 2：Background and Motivation

总体结构保留：

1. Previous Solutions；
2. On-Policy Distillation Mechanism。

修改原则：

- OPD 的定义保持 method-agnostic：student-generated rollout → teacher/evaluator signal → student update；
- 不把 token-level KL 写成 OPD 唯一机制；
- “training on student states reduces mismatch”写成机制动机，不写成 EasyOPD 自己证明的结论；
- 避免在 Section 2 重复 Table 1 和 Section 3 的框架贡献；
- 控制在约 338–430 词，把篇幅留给 running example 和系统实验。

---

## 7. Section 3：Design and Implementation

保留以下四个 subsection：

1. A Unified OPD Workflow；
2. Declarative Invocation and Configuration；
3. Method-Local Supervision Logic；
4. Execution through Thin verl Hooks。

### 7.1 Section 3.1

将 `Figure ??` 改成真实引用。推荐正文：

> Given a student, a teacher or evaluator, and training prompts, the student first generates on-policy responses or trajectories. The selected method module constructs supervision on the resulting prefixes or states, and verl performs the corresponding optimization. EasyOPD standardizes the invocation and dispatch process; it does not require all methods to share a single loss, teacher interface, or evaluation metric.

最后一句很重要，用来避免“统一框架 = 强迫所有方法使用统一 objective”的误解。

### 7.2 Section 3.2

将：

> Switch methods by changing YAML only

限制为：

> Released methods can be selected through method-specific YAML configurations without manually editing trainer dispatch logic.

必须配 Figure 2 的真实 YAML/CLI。

### 7.3 Section 3.3

将 diagnostics 从设想写成可核验实现：

> Implemented method modules expose supervision-specific diagnostics in addition to task performance. For example, the released cross-tokenizer implementation reports [actual logged metrics], while the step-wise implementation reports [actual logged metrics].

若只是计划支持，不能写 `reports`，应删除或写 limitations。

### 7.4 Section 3.4

保留 hook 是 dispatch boundary 的解释。修改 teacher-sidecar claim：

- 只有真实实现的信号才能举例；
- 若当前只传 teacher logits/log-probabilities 和 judge outputs，不得写 hidden states；
- 明确分布式能力来自 verl：

> EasyOPD inherits rollout generation, worker orchestration, optimization, and distributed execution from verl; its contribution is the dispatch layer that connects these components to method-local supervision logic.

删除无实验证据的 `at scale`，或者在加入 scaling results 后保留。

---

## 8. Table 1：框架比较表

### 8.1 修改列名

建议把主观列替换成可核验列：

- `Simple & reproducible invocation` → `Released YAML/CLI examples`
- `Method-local extensibility` → `Documented method extension API`
- `Large-scale async multi-node` → `Compatible with multi-node execution`

EasyOPD 的多节点能力写成：

> ✓ (via verl)

不能让读者误以为 EasyOPD 重新实现了分布式后端。

### 8.2 版本证据

Caption 增加：

> The comparison reflects publicly documented functionality as of [date], using the versions or commits listed in Appendix A.

Appendix A 必须列：

- framework version/commit；
- official documentation或代码链接；
- 每个 ✓、◦、× 的可核验依据。

如果无法核验某一格，使用 `not documented`，不要直接推断为 `×`。

---

## 9. Section 4：Experiments

实验章节按以下结构组织：

### 4.1 Evaluation Scope and Setup

开头明确三类 setting 是为了覆盖不同 supervision interfaces，而不是跨表直接比较算法优劣：

> The three settings are not intended as a single cross-regime leaderboard. They exercise different supervision spaces, teacher-conditioning schemes, and supervision granularities under a shared implementation structure.

#### Cross-tokenizer OPD

保留当前模型、数据和指标，但必须补：

- 10K warm-start corpus 的来源与构造；
- sampling 参数；
- checkpoint selection；
- 训练硬件与预算；
- exact match/pass@1 的实现。

#### On-policy self-distillation

统一写法：

> On-policy self-distillation (OPSD) uses the same base model under different conditioning contexts. We instantiate this setting with SDPO ...

后续所有具体结果均使用 SDPO：

- `We implement SDPO ...`
- `Table 3 compares SDPO and GRPO ...`
- `SDPO is higher/lower ...`

不得写 “OPSD is 19.2 points higher”，因为 OPSD 是范式而不是表格中的具体方法。

Table 3 caption 改为：

> SDPO and GRPO results under one-hour and five-hour wall-clock budgets. Average@16 is the mean correctness over 16 independently sampled responses per question. Base denotes the untrained starting checkpoint and is therefore reported once per task. The highest observed score for each task and budget is shown in bold.

如果 “highest observed” 是在测试集上多次选择 checkpoint，必须如实补充 evaluation frequency 和选择规则。不能暗示它是固定 1h/5h checkpoint。

#### Step-wise OPD

全文统一为：

> We use Qwen3-1.7B as the student and a GRPO-optimized Qwen3-4B as the teacher.

Table 4 caption 改为：

> Comparison of GRPO, response-level OPD, and SOD using Qwen3-1.7B as the student. OPD and SOD use a GRPO-optimized Qwen3-4B as the teacher. Results are reported as percentages; Avg. is the unweighted mean across AIME 2024, AIME 2025, and LiveCodeBench v6.

### 4.2 End-to-End OPD Case Studies

保留当前 Table 2–4 数值，但压缩逐格复述。每个 case study 用两类结论：

1. framework conclusion：该方法使用哪些 hook、是否在同一个 verl backend 中运行；
2. empirical observation：当前方法结果的准确、局部描述。

推荐 overall finding：

> Across the three case studies, EasyOPD executes the evaluated methods through the same verl-based backend while preserving their distinct alignment, teacher-conditioning, and supervision-granularity requirements. The results should be interpreted within each setting rather than as a cross-regime ranking.

不得写：

- all OPD methods；
- universal evaluation protocol；
- agentic environments；
- algorithmic superiority caused by EasyOPD。

### 4.3 System Efficiency（必须新增）

做 matched comparison：同一个 standard OPD 方法、相同模型、相同 batch、相同硬件。

至少报告：

| Implementation | Tokens/s | Step time | Peak VRAM | Wall-clock | Notes |
|---|---:|---:|---:|---:|---|
| Direct/matched verl | real | real | real | real | baseline |
| EasyOPD | real | real | real | real | same method |

将开销拆分为：

- framework dispatch overhead；
- teacher inference；
- alignment/reward evaluation。

只有真实测量后才能恢复 `[K]% overhead` claim。如果来不及测量，删除 Introduction 中的 overhead 数字承诺，不得估计。

### 4.4 Extensibility Analysis（必须新增）

至少比较一个 alignment-heavy 方法（SimCT）和一个 trajectory/step-heavy 方法（SOD 或 SDPO）：

| Method | Core verl files modified | Method-local files | Added LOC | Hooks used | Config lines |
|---|---:|---:|---:|---|---:|
| SimCT | real | real | real | alignment, teacher, loss | real |
| SDPO/SOD | real | real | real | actual hooks | real |

统计规则：

- 只统计 method-specific changes；
- shared hook layer 单独报告一次，不重复计入每个方法；
- generated code、tests、comments 是否计入要写清楚；
- 所有数字从 git diff 或仓库实际文件统计，不得人工估算。

只有该表证明所有 evaluated methods 均无 core changes 后，才能写 “no further changes to core trainer or worker code”。

---

## 10. Results 段落的准确化替换

### 10.1 Cross-tokenizer

可保留：

> All four evaluated cross-tokenizer methods improve the unweighted average over the base student in this setting. SimCT obtains the highest observed average, while the methods show different task-specific strengths.

将框架结论限制为：

> These results verify that the evaluated cross-tokenizer objectives can be executed through EasyOPD's alignment and loss interfaces under the matched setup.

不要写 EasyOPD 导致了这些提升。

### 10.2 OPSD/SDPO

必须写：

> Table 3 compares SDPO and GRPO in the OPSD evaluation setting.

后续所有数值主语必须是 SDPO，不是 OPSD。

结论限制为：

> SDPO's relative performance is task-dependent in the evaluated setting: it obtains its largest gains on Chemistry and Biology, while GRPO is stronger on GSM8K and on the five-hour Tool Use result.

### 10.3 Step-wise OPD

按当前结果保留：

> SOD obtains 43.65, 38.33, and 37.88 on AIME 2024, AIME 2025, and LiveCodeBench v6, respectively, for an unweighted average of 39.95. Relative to response-level OPD, the corresponding gains are 5.52, 2.29, and 7.61 points.

后接：

> Under this setup, the results are consistent with the intended role of step-wise weighting; they do not by themselves establish that every step-wise OPD method will outperform response-level OPD.

---

## 11. Conclusion：建议整体替换

> We present EasyOPD, a method-oriented framework for integrating on-policy distillation methods with a shared verl execution backend. EasyOPD separates user-side configuration, method-specific supervision construction, and distributed rollout and optimization. Through case studies in cross-tokenizer OPD, on-policy self-distillation, and step-wise OPD, we show that the evaluated methods can retain their distinct supervision requirements within the same implementation structure. EasyOPD does not replace verl's execution infrastructure; rather, it provides method-local extension boundaries and reproducible invocation on top of it. We release the framework with runnable configurations, documentation, an installable demonstration package, and a demonstration video. Future work will expand the released method coverage and strengthen diagnostics and system-level evaluation.

---

## 12. Limitations 与 license

建议在 Conclusion 前加入一个短 Limitations paragraph：

> EasyOPD currently depends on verl and has been validated on a representative, rather than exhaustive, set of OPD supervision interfaces. The three experimental settings use different models, tasks, and metrics and should not be interpreted as a single cross-regime leaderboard. Teacher inference, tokenizer alignment, and external evaluation can introduce method-specific costs beyond the framework dispatch overhead. Although the extension boundaries are designed to accommodate additional supervision forms, support should be claimed only after the corresponding implementation and runnable configuration are released.

在 Introduction 末段、artifact 段或 footnote 明确写：

> EasyOPD is released under the [actual repository license].

不得猜测 MIT/Apache-2.0；从 repo 的 LICENSE 文件读取真实许可证。

---

## 13. Demo package 与 2.5 分钟视频

### Demo package 最低要求

README 必须包含：

1. installation；
2. hardware/software requirements；
3. 一个可在合理资源下运行的 small-scale example；
4. 一个真实 YAML；
5. 一条复制即可运行的命令；
6. 预期输出；
7. diagnostics 解释；
8. license；
9. tested commit/version。

### 视频推荐脚本

- 0:00–0:20：OPD 实现碎片化问题；
- 0:20–0:45：三层架构；
- 0:45–1:15：选择 YAML 并启动一个真实任务；
- 1:15–1:45：展示 registry、激活 hooks 和 diagnostics；
- 1:45–2:10：切换到另一类 supervision method；
- 2:10–2:30：结果、GitHub、安装方式与 license。

视频必须展示真实软件运行，不要只播放论文 PPT。

---

## 14. CodeBuddy 执行顺序

### Phase A：事实与一致性修复

1. 全局统一 GRPO-optimized Qwen3-4B teacher；
2. 建立 OPSD（范式）/SDPO（方法）术语映射并全局修改；
3. 保留当前 Table 2–4 数值；
4. 清除所有 placeholder、无效引用和无效 URL；
5. 核对 license、repo、video、demo package。

### Phase B：claim audit

对每个 `support / reproduce / standardize / scale / async / easy / all / only / without / no further changes / agentic` 做全文审计：

- 能由实验直接支持：保留，并指向表/图；
- 能由代码支持但本文未实验：改成 implementation claim；
- 只是继承自 verl：明确写 inherited/reused；
- 只是设计意图：改成 designed to accommodate；
- 没有代码或实验：删除。

### Phase C：图和系统证据

1. 按第3节修改 PPT Slide 1；
2. 新增 running-example Slide 2；
3. 加入 system efficiency；
4. 加入 extensibility table；
5. 压缩算法表格结果的逐格复述，为系统证据腾出篇幅。

### Phase D：最终 QA

执行全文检查：

```text
Qwen3-8B（step-wise teacher）      = 0
OPSD 作为方法行/方法结果主语       = 0
agentic environments/tasks        = 0，除非新增真实环境实验
[X/Y/Z/N/M/K]                     = 0
Figure ?? / Appendix ??           = 0
[define Avg@16]                   = 0
无效 URL                           = 0
```

然后重新编译 PDF，逐页检查：

- Figure 1/2 字体在双栏论文中可读；
- 表格没有越界；
- 正文不超过6页；
- appendix不超过2页；
- 所有 caption 与正文数字一致；
- 所有方法名、模型名和 benchmark 名一致。

---

## 15. 最终验收标准

修改完成后的论文必须能清楚回答：

1. EasyOPD 面向谁？
2. 它解决的不是 OPD 算法性能，而是哪种系统集成问题？
3. 一个用户如何从 YAML 启动真实训练？
4. 一个新方法需要实现哪些扩展边界？
5. 哪些能力由 EasyOPD 提供，哪些能力继承自 verl？
6. 已发布并评测了哪些方法，哪些只是未来可能支持？
7. EasyOPD 增加了多少运行开销？
8. 相比直接修改 verl，减少了多少 core modification？
9. OPSD 与 SDPO 的关系是否全文一致？
10. Step-wise teacher 是否全文统一为 GRPO-optimized Qwen3-4B？

如果第7和第8项没有真实数据，应删除相应量化 claim，而不是留下 placeholder 或估计值。

## 参考对照

- EMNLP 2026 System Demonstrations Call: https://2026.emnlp.org/calls/demos/
- EasyEdit, ACL 2024 System Demonstrations: https://aclanthology.org/2024.acl-demos.9/
- RAGLAB, EMNLP 2024 System Demonstrations: https://aclanthology.org/2024.emnlp-demo.43/
- MarkLLM, EMNLP 2024 System Demonstrations: https://aclanthology.org/2024.emnlp-demo.7/
- EasyEdit2, EMNLP 2025 System Demonstrations: https://aclanthology.org/2025.emnlp-demos.38/